import { Inter, Lexend, Darker_Grotesque } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
});

export const lexend = Lexend({
  subsets: ["latin"],
});

export const darkGrotesque = Darker_Grotesque({
  subsets: ["latin"],
});
